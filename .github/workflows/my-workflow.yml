name: github-context-demo

on:
    push:
    workflow_dispatch:
    
env: 
    var1: "pavan"
    var2: "pathan"

jobs:

    job1:

      runs-on: ${{vars.imageVersion}}

      env: 
        var3: "akhil"
        var2: "pathan basha khan"

      steps:
      - name: step1
        env:
            var4: "digistack"
            var1: "pavan ramana"
        run: |
            echo "hii ${{env.var1}}" 
            echo "byee ${{env.var2}}"
            echo "ok ${{env.var3}}"
            echo "employees of  ${{env.var4}}"

      - name: step2
        run: |
            echo "${{vars.message}}"



    