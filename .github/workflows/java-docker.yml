name: java-docker
on:
  workflow_dispatch:


jobs:
    run-java-docker:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - name: using docker action
              uses: ./.github/actions/java-docker/