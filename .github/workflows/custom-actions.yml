name: my-custom-actions

on:
    workflow_dispatch:

jobs:
    my-custom-action:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: using composite action
              uses: ./.github/actions/composit-actions/
              