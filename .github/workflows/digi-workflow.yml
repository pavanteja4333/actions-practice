name: vars demo

on:
    push:
        branches: ["main"]
    workflow_dispatch:
        inputs:
            version:
                type: choice
                options:
                    - v1
                    - v2
                    - v3
                default: v1

jobs:
    myjob:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: |
                echo "hi"