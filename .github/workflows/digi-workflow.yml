name: vars demo
run-name: ${{github.run_id}}-pavan

on:
    push:
        branches: ["main"]
    workflow_dispatch:
        inputs:
            version:
                type: choice
                options:
                    - v1
                    - v2
                    - v3
                default: v1

jobs:
    myjob:
        runs-on: ubuntu-latest
        environment: dev
        steps:
            - name: step1
              run: |
                echo "hi from ${{inputs.version}}"
                echo "${{vars.my_dev_user}}"