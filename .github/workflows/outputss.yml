name: techbyte-wf

on:
    push:

jobs:
    job1:

        runs-on: ubuntu-latest

        steps:
            - name: step1
              id: pv 
              run: echo "name = pavan" >> "$GITHUB_OUTPUT"

            - name: step2
              run: echo "${{steps.pv.outputs.name}}"